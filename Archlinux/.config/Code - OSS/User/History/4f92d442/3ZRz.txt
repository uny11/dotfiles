📂 Ejemplo práctico:

Copiar una carpeta local al NAS:
bash

rsync -avz -e "ssh -p 22" /home/tu_usuario/fotos/ admin@192.168.1.100:/volume1/fotos_backup/

Descargar del NAS a tu PC:
bash

rsync -avz -e "ssh -p 22" admin@192.168.1.100:/volume1/fotos_backup/ /home/tu_usuario/descargas/

🔧 Requisitos:

    Tener rsync instalado en tu PC Linux (normalmente viene por defecto). Si no:
    bash

sudo apt install rsync  # Debian/Ubuntu
sudo dnf install rsync # Fedora

Conexión SSH activa en el NAS (como ya configuraste).